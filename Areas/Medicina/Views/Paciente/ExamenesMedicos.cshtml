@using ProyectoProgramadoLenguajes2024.Models.ViewModels
@model MedicamentosPacientesVM
@{
    ViewData["Title"] = "Expediente - Examenes Medicos";
    var pacienteId = ViewData["PacienteId"];
}

<partial name="_Notifications" />

<div class="container">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">
                Exámenes Médicos
            </h2>
        </div>
        <div class="col-6 text-end">
            <a asp-area="Medicina" asp-controller="Paciente" asp-action="AgregarExamenesMedicos" asp-route-id="@pacienteId" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> Agregar Examen Médico
            </a>
        </div>
    </div>

    <input type="hidden" id="pacienteId" value="@pacienteId" />

    <table id="tblData" class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th>Descripción</th>
                <th>Realizado Por</th>
                <th>Acciones</th>
            </tr>
        </thead>
    </table>
</div>

<script src="~/lib/jquery/dist/jquery.min.js"></script>
<script src="~/js/examenMedico.js"></script>